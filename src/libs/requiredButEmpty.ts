import { FormWithQuestions } from "../types/formType";
import { PayloadQuestionAnswers } from "../types/payloadType";

export const requiredButEmpty = async (form: FormWithQuestions[], payload: PayloadQuestionAnswers) =>
{
    

}